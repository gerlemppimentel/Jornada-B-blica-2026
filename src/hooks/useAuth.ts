[{'react': 'import { useNavigate'}, {'@/integrations/supabase/client': 'interface UserProfile {\n  id: string;\n  first_name: string;\n  congregation: string;\n  user_role: string;\n  congregation_id: string | null;'}, {'data': {'data': 'authListener', 'perfil': ", error);\n          } else {\n            setProfile(profileData as UserProfile);\n          }\n        }\n      } catch (err) {\n        console.error('Erro ao buscar usuário:"}, 'SIGNED_IN': 'session?.user) {\n        fetchUserProfile();'}, {'admin': ''}, {"admin') {\n      // Se não for admin, redireciona para a página principal\n      navigate('/": 'return false;'}, {}]